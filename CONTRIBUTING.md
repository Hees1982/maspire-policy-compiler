# Contributing

1. Create a branch from `main`.
2. Add tests for compiler, evaluator and trace changes.
3. Run `ruff check app tests` and `pytest`.
4. Never commit customer SOPs, footage, personal data or secrets.
5. Treat operator semantics and canonical checksum changes as compatibility changes.

